# MATLAB
*.asv
*.m~
*.mex*
*.mlappinstall
*.mltbx

# macOS
.DS_Store
._*

# Temporary
*.tmp
*.bak

# Raw data — not tracked (download from OSF, see README)
data/raw/

# Uncompressed haptic data (use .gz version)
data/preprocessed/Haptics/HapticFeedback.csv

zenodo_README.md

# Claude settings
data/.claude/

# Preprocessed data archive (available on Zenodo)
data/preprocessed.zip

# Preprocessed EEG data — regeneratable via preprocessing scripts
data/preprocessed/EEG/*.mat
data/preprocessed/EEG/*.npz
data/preprocessed/EEG/pce*/

# Python build artifacts
__pycache__/
*.pyc
*.pyo

# Aperiodic-exponent per-participant intermediates (regenerated by
# code/analysis/computeAperiodicExponent.py from the *-raw.fif files;
# the within-trial cohort summary that the paper references IS tracked).
results/aperiodic_exponent_per_participant*.csv
results/aperiodic_exponent_per_participant*.json

# Within-trial complexity intermediates (regenerated by
# code/analysis/computeWithinTrialComplexity.py from the *-raw.fif files;
# the small decoupling_bayes_factors.csv summary the paper references IS tracked).
results/within_trial_complexity_pertrial.csv
results/within_trial_windows.csv
